% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/winsorize.R
\name{winsorize}
\alias{winsorize}
\title{Winsorize outliers using a robust MAD threshold}
\usage{
winsorize(x, threshold = 3.5, constant = 1.4826, na_rm = FALSE)
}
\arguments{
\item{x}{A numeric vector.}

\item{threshold}{A finite positive number. Values with \code{abs(mad_score(x)) > threshold} are capped at the corresponding boundary.}

\item{constant}{A positive scaling constant passed to \code{\link[stats:mad]{stats::mad()}}.}

\item{na_rm}{Logical. Should missing values be removed?}
}
\value{
A numeric vector the same length as \code{x}, with outliers capped.
}
\description{
Caps values whose robust MAD score (see \code{\link[=mad_score]{mad_score()}}) exceeds a
threshold in absolute value, replacing them with the nearest boundary
value rather than removing them.
}
\examples{
winsorize(c(1, 2, 2, 3, 100))
}
