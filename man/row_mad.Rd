% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_mad.R
\name{row_mad}
\alias{row_mad}
\title{Calculate MAD across rows or columns of a matrix}
\usage{
row_mad(x, margin = 1, na_rm = FALSE, constant = 1.4826)
}
\arguments{
\item{x}{A numeric matrix.}

\item{margin}{\code{1} to compute MAD across rows (the default; e.g. one row
per feature), or \code{2} to compute across columns.}

\item{na_rm}{Logical. Should missing values be removed?}

\item{constant}{A positive scaling constant passed to
\code{\link[matrixStats:rowMads]{matrixStats::rowMads()}}/\code{\link[matrixStats:colMads]{matrixStats::colMads()}}.}
}
\value{
A numeric vector of MAD values, one per row (or column) of \code{x},
named with the corresponding row (or column) names if present.
}
\description{
Computes the median absolute deviation for each row (or column) of a
numeric matrix, e.g. to rank variable genes, proteins, or other features
in an omics matrix. Uses \code{\link[matrixStats:rowMads]{matrixStats::rowMads()}}/\code{\link[matrixStats:colMads]{matrixStats::colMads()}}
internally for speed.
}
\examples{
m <- matrix(c(1, 2, 2, 3, 100, 10, 20, 20, 30, 40), nrow = 2, byrow = TRUE)
row_mad(m)
}
