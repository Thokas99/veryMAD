% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mad-qc-wrappers.R
\name{mad_qc_bulk}
\alias{mad_qc_bulk}
\title{Bulk observation-level MAD quality control}
\usage{
mad_qc_bulk(
  data,
  metrics = NULL,
  nmads = 3,
  transform = "log1p",
  group_by = NULL,
  verbose = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A matrix, data frame, or tibble with observations in rows.}

\item{metrics}{Named character vector mapping columns to \code{"lower"},
\code{"upper"}, or \code{"both"}. Required; veryMAD never guesses metrics.}

\item{nmads}{Positive number of MADs used for thresholds.}

\item{transform}{\code{"log1p"}, \code{"none"}, \code{"identity"}, or \code{"log10"}, or a
named partial override. Unnamed metrics in an override use \code{"log1p"}.}

\item{group_by}{Optional unique column names for within-group calculations.}

\item{verbose}{Print a concise QC summary?}

\item{overwrite}{Replace existing per-metric flag columns?}
}
\value{
The input as a data frame with one logical \verb{*_mad_outlier} column per
metric. The compact report is available as \code{attr(result, "mad_qc")}.
}
\description{
Rows are observations and \code{metrics} explicitly selects numeric QC columns.
A gene-by-sample expression matrix is not automatically a QC-metric table.
Selected metrics use \code{log1p} unless \code{transform} is \code{"none"} or supplies a
named partial override. Measurements are never changed or filtered.
}
\examples{
d <- data.frame(size = c(10, 11, 12, 100), rate = c(.9, .91, .92, .2))
out <- mad_qc_bulk(d, c(size = "upper", rate = "lower"),
                   transform = c(rate = "none"), verbose = FALSE)
attr(out, "mad_qc")$thresholds
}
