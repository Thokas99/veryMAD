% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mad-qc-wrappers.R
\name{mad_qc_sc}
\alias{mad_qc_sc}
\title{Single-cell observation-level MAD quality control}
\usage{
mad_qc_sc(
  object,
  metrics = NULL,
  nmads = 3,
  transform = "log1p",
  group_by = NULL,
  verbose = TRUE,
  overwrite = FALSE,
  action = c("annotate", "report")
)
}
\arguments{
\item{object}{A data frame, tibble, or Seurat object.}

\item{metrics}{Named character vector mapping columns to \code{"lower"},
\code{"upper"}, or \code{"both"}. Required; veryMAD never guesses metrics.}

\item{nmads}{Positive number of MADs used for thresholds.}

\item{transform}{\code{"log1p"}, \code{"none"}, \code{"identity"}, or \code{"log10"}, or a
named partial override. Unnamed metrics in an override use \code{"log1p"}.}

\item{group_by}{Optional unique column names for within-group calculations.}

\item{verbose}{Print a concise QC summary?}

\item{overwrite}{Replace existing per-metric flag columns?}

\item{action}{Annotate the input or return the compact report.}
}
\value{
For \code{action = "report"}, a compact \code{mad_qc_result}. Otherwise the
metadata data frame or Seurat object with one \code{mad_qc_outlier} column. The
data-frame annotation stores the report in \code{attr(result, "mad_qc")}.
}
\description{
Accepts cell metadata or a Seurat object. Annotation adds only
\code{mad_qc_outlier}; individual flags remain in the compact report. \code{TRUE} means
at least one metric failed, \code{FALSE} means all passed, and \code{NA} means none
failed but at least one could not be evaluated. Cells are never filtered.
}
\examples{
cells <- data.frame(counts = c(NA, 10, 11, 100), mt = c(2, 3, 4, 30))
mad_qc_sc(cells, c(counts = "both", mt = "upper"),
          transform = c(mt = "none"), action = "report", verbose = FALSE)
}
