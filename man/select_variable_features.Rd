% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_variable_features.R
\name{select_variable_features}
\alias{select_variable_features}
\title{Select the most variable features by MAD}
\usage{
select_variable_features(
  x,
  n = 2000,
  margin = 1,
  na_rm = FALSE,
  constant = 1.4826
)
}
\arguments{
\item{x}{A numeric matrix.}

\item{n}{The number of features to select.}

\item{margin}{\code{1} to rank rows (the default; e.g. one row per feature),
or \code{2} to rank columns.}

\item{na_rm}{Logical. Should missing values be removed?}

\item{constant}{A positive scaling constant passed to \code{\link[=row_mad]{row_mad()}}.}
}
\value{
A character vector of the top \code{n} row (or column) names, ordered
by decreasing MAD. If \code{x} has no names on that margin, an integer
vector of indices is returned instead.
}
\description{
Ranks features (rows, or columns if \code{margin = 2}) of a matrix by
variability and returns the top \code{n}. A thin convenience wrapper around
\code{\link[=row_mad]{row_mad()}}.
}
\examples{
m <- matrix(rnorm(50), nrow = 10)
rownames(m) <- paste0("gene", 1:10)
select_variable_features(m, n = 3)
}
