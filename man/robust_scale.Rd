% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust_scale.R
\name{robust_scale}
\alias{robust_scale}
\title{Median-center and MAD-scale a matrix}
\usage{
robust_scale(
  x,
  margin = 1,
  center = TRUE,
  scale = TRUE,
  zero_mad = c("zero", "na", "error")
)
}
\arguments{
\item{x}{A numeric matrix.}

\item{margin}{\code{1} to scale across rows (the default; e.g. one row per
feature), or \code{2} to scale across columns.}

\item{center}{Logical. Should each row/column be median-centered?}

\item{scale}{Logical. Should each row/column be MAD-scaled?}

\item{zero_mad}{What to do with rows/columns whose MAD is 0 (common in
sparse omics data, where a feature can be constant within a group).
One of \code{"zero"} (set scaled values to 0, the default), \code{"na"} (set
scaled values to \code{NA}), or \code{"error"} (abort).}
}
\value{
A numeric matrix the same shape as \code{x}.
}
\description{
Robustly standardizes each row (or column) of a matrix by subtracting the
median and dividing by the median absolute deviation, analogous to
\code{\link[base:scale]{base::scale()}} but resistant to outliers. Well suited to expression
matrices, where rows are typically features (genes, proteins, miRNAs) and
columns are samples.
}
\examples{
m <- matrix(c(1, 2, 2, 3, 100, 10, 20, 20, 30, 40), nrow = 2, byrow = TRUE)
robust_scale(m)
}
\seealso{
Other robust MAD helpers:
\code{\link[=is_outlier]{is_outlier()}},
\code{\link[=mad_qc]{mad_qc()}},
\code{\link[=mad_score]{mad_score()}},
\code{\link[=row_mad]{row_mad()}},
\code{\link[=select_variable_features]{select_variable_features()}},
\code{\link[=winsorize]{winsorize()}}
}
\concept{robust MAD helpers}
